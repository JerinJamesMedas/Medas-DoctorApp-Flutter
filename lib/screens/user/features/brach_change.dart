import 'package:flutter/material.dart';

class BranchChangePage extends StatelessWidget {
  const BranchChangePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Branch Change")));
  }
}